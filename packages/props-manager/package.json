{
  "name": "@asra/props-manager",
  "version": "1.0.1",
  "description": "A structured property management library that provides API-based control over prop components, separate from the scene tree.",
  "author": "Asa Tsai",
  "license": "MIT",
  "packageManager": "yarn@4.3.1",
  "keywords": [],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build:props-manager": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest",
    "test:ci": "vitest run --coverage --reporter=default --reporter=junit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@asra/reactive-events": "workspace:*",
    "@asra/utils": "workspace:*"
  }
}
