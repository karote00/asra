import { addRectangle } from '@asra/reactive-events'
import sceneTree, { SceneTree } from '@asra/scene-tree'

export default class SceneTreeManager {
  sceneTree: SceneTree = sceneTree

  constructor() {}

  addRectangle() {
    addRectangle()
  }
}
