{
  "version": 2,
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "installCommand": "yarn install --immutable",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
}
