# Flow Inspector Release Decision History

`unreleased.md` is the append-only decision log for Flow Inspector work not yet
published as a tagged tool release. A future release cut may snapshot it into a
versioned history file; publication requires separate explicit authorization.
