# Conversational AI Cat Portrait Visual Records

This directory preserves the user-requested synchronized live-app screenshots
for each detailed cat portrait iteration. Files are numbered in generation
order and include the ordinary editable element count in the filename.

The `research/` subdirectory preserves source images, vectorizer SVG output,
and rendered previews used to compare image-to-vector techniques. Temporary
tool installations are excluded from these records.
