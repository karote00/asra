import core from './core'

const render = core.deps.render
const systemContext = core.deps.systemContext
const sceneTree = core.deps.sceneTree
const factory = core.deps.factory

export {
  render,
  systemContext,
  sceneTree,
  factory
}

export default core
