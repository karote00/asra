{
  "version": 2,
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "installCommand": "yarn install --focus --all --immutable",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
}
