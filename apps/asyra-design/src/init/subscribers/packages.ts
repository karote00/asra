/**
 * Subscribers - Package-level subscriber initialization
 * These subscribers initialize YJS observers from packages
 */

// Note: @asyra/selection package initializes its subscribers on import
// via packages/selection/src/index.ts line 6: initSelectionSubscribes()
// The subscribers are already active when the package is imported

export const initPackageSubscribers = () => {
  // Package subscribers are auto-initialized on import
  // This file is a placeholder for future package subscriber initialization
}
