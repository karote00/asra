import { PositionComponent, DimensionComponent } from './components'

const PropClassMap = {
  position: PositionComponent,
  dimension: DimensionComponent
}

export const createProperty = (propName: string) => {}
